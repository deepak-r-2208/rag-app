# Wherever the FastAPI backend is running.
# docker-compose sets this automatically at build time — this file only
# matters if you run the frontend outside Docker (npm run dev).
VITE_API_BASE_URL=http://localhost:8000
